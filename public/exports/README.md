# Wali Tahfiz brand exports

These are the production-ready, lossless SVG masters:

- `wali-tahfiz-logo.svg` — horizontal wordmark for headers, decks, and print.
- `wali-tahfiz-app-icon.svg` — square app mark for product surfaces.

The install-ready PNG icons remain in `public/icons/`:

- `app-icon-180.png`
- `app-icon-192.png`
- `app-icon-512.png`
- `app-icon-maskable-512.png`

Use the SVG files whenever the destination supports vector artwork. They preserve the original geometry and brand colors at any resolution.
