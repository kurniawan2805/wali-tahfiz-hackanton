# Simpan key asli hanya di environment deployment/server, jangan di frontend.
OPENAI_API_KEY=your_openai_api_key_here

# Optional: default interface language for new visitors (id or en).
VITE_APP_LOCALE=id
